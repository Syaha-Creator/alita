<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.syahrul.alitapricelist.alitapricelist">

    <!-- Galeri: Android 12 ke bawah (image_picker fallback) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>

    <!-- Kamera: foto struk pembayaran (checkout) -->
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- Jaringan: API, login, dll. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Notifikasi: wajib Android 13+ (API 33) agar FCM bisa tampilkan push -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Geotagging approval: rekam lokasi saat atasan setuju/tolak -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- AD_ID: wajib Android 13+ (API 33) agar Firebase Analytics berfungsi normal -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"/>

    <!-- Haptic feedback: wajib di beberapa ROM (MIUI/Xiaomi) -->
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:label="Alitapricelist"
        android:name="${applicationName}"
        android:icon="@mipmap/logo">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:enableOnBackInvokedCallback="true">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Deep Links: App Links via Firebase Hosting -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="https"
                    android:host="alita-pricelist-12d76.web.app"
                    android:pathPrefix="/product"/>
                <data
                    android:scheme="https"
                    android:host="alita-pricelist-12d76.firebaseapp.com"
                    android:pathPrefix="/product"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Default channel for FCM when payload does not set channel_id -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="approval_channel" />
    </application>

    <!-- Untuk share / kirim gambar (share_plus, dll.) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.SEND"/>
            <data android:mimeType="image/*"/>
        </intent>
    </queries>
</manifest>
