#include? "Pods/Target Support Files/Pods-Runner/Pods-Runner.release.xcconfig"
#include "Generated.xcconfig"
// Secrets dari .env — di-generate `gen_xcconfig` di scripts/release.sh.
// File ini di-include SETELAH Generated.xcconfig dan MENIMPA DART_DEFINES;
// jadi semua --dart-define release (termasuk INDIRECT_*) harus ada di sini.
#include? "DartSecrets.xcconfig"
